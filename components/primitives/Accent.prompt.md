Italic Newsreader accent word inside an Archivo headline; the system's one flourish. One word or short phrase per headline, once per screen.

```jsx
<h1>Agents that <Accent underline>ship</Accent></h1>
```

`underline` adds the thick accent underline (hero only). `ink` keeps it ink-colored for quieter contexts.
