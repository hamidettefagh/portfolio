Hairline separator, the system's primary way to divide content (instead of boxes or background changes).

```jsx
<Rule />
<Rule label="Now" strong />
```

`label` renders a mono accent label before the line. `strong` uses the darker hairline.
