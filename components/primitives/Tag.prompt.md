Small mono pill for industry descriptors ("Major US airline") and metadata. Use one or two per row, never dense badge grids.

```jsx
<Tag>Global dining platform</Tag>
<Tag variant="accent">Hackathon, top 5 of 63</Tag>
```

Variants: `outline` (default), `fill` (paper inset), `accent` (orange wash, use sparingly).
